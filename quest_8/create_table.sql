CREATE TABLE  dogs (
	id		integer	 	NOT NULL,
	name		VARCHAR(50)	NOT NULL,

	PRIMARY KEY (id)
);

CREATE TABLE  cats (
        id              integer         NOT NULL,
        name            VARCHAR(50)     NOT NULL,

        PRIMARY KEY (id)
);

